<nav class="bg-blue-800 shadow-lg">
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-between h-16">
      <div class="flex-shrink-0">
        <%= link_to "Zaytoon", root_path, class: "text-white px-3 py-2 rounded-md text-lg font-bold" %>
      </div>
      <div class="flex-1 flex items-center justify-end sm:items-stretch sm:justify-end">
        <div class="hidden sm:flex space-x-4">
          <%= link_to "Home", root_path, class: "text-gray-50 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "About", about_path, class: "text-gray-100 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "Categories", categories_path, class: "text-gray-100 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          <%= link_to "Contact", contact_path, class: "text-gray-100 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          <% if user_signed_in? %>
            <%= link_to "Edit profile", edit_user_registration_path, class: "text-gray-100 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "Sign out", destroy_user_session_path, method: :delete, data: { confirm: "Are you sure?" }, class: "text-gray-100 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "text-gray-100 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
            <%= link_to "Log in", new_user_session_path, class: "text-gray-100 hover:bg-blue-600 hover:text-white px-3 py-2 rounded-md text-sm font-medium" %>
          <% end %>
        </div>
        <div class="absolute inset-y-0 right-0 flex items-center sm:hidden">
          <button type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-controls="mobile-menu" aria-expanded="false">
            <span class="sr-only">Open main menu</span>
            <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"/>
            </svg>
            <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>
</nav>
